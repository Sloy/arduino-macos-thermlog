var SerialPort = require('serialport');

// List serial ports:
/*
SerialPort.list()
    .then((ports) => {
        ports.forEach(function (port) {
            console.log(port.path);
        });
    })

    */
